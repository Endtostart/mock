package org.mock.client.transfer;

import org.mock.common.transfer.Caller;
import org.mock.common.transfer.Message;

public class HttpCaller<T> implements Caller<T> {

    @Override
    public T call(Message message) {
        return null;
    }
}
